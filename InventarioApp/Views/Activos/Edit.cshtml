@model InventarioApp.Models.Activo

@{
    ViewBag.Title = "Modificar activo";
    ViewBag.EsCreacion = false;

    if (Request.IsAjaxRequest()) { Layout = null; }
}

<div class="modal-header">
    <div>
        <h5 class="modal-title">Modificar activo</h5>
        <div class="text-muted-sm">@Model.Nombre <span class="badge-codigo ms-1">@Model.Codigo</span></div>
    </div>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
</div>

@using (Html.BeginForm("Edit", "Activos", FormMethod.Post, new { id = "activoForm" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)

    @Html.Partial("_ActivoFormCard", Model)

    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <button type="submit" class="btn btn-primary">Guardar cambios</button>
    </div>
}
