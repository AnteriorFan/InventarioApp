@model IEnumerable<InventarioApp.Models.Marca>
@{ ViewBag.Title = "Marcas"; }

<h2>Marcas</h2>
<a href="@Url.Action("Create")" class="btn btn-primary mb-3">+ Nueva marca</a>

<table class="table table-striped">
    <thead><tr><th>Nombre</th><th>Descripción</th><th>Acciones</th></tr></thead>
    <tbody>
        @foreach (var m in Model)
        {
            <tr>
                <td>@m.Nombre</td>
                <td>@m.Descripcion</td>
                <td>
                    <a href="@Url.Action("Edit", new { id = m.Id })" class="btn btn-sm btn-outline-secondary">Editar</a>
                    @using (Html.BeginForm("Delete", "Marcas", FormMethod.Post, new { onsubmit = "return confirm('¿Eliminar esta marca?');", style = "display:inline" }))
                    {
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@m.Id" />
                        <button type="submit" class="btn btn-sm btn-outline-danger">Eliminar</button>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
